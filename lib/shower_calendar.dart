import 'package:flutter/material.dart';

// Shows the showering calendar
class ShowerCalendar extends StatelessWidget {
  const ShowerCalendar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
